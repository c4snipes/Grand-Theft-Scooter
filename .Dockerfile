# Symlink placeholder to docker/Dockerfile
# This file is intentionally left as a symlink to docker/Dockerfile in CI/local builds.
# If your OS doesn't support symlinks in the repo, ensure the CI runner has access to docker/Dockerfile.
